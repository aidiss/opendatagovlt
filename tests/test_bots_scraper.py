import opendatagovlt

def test_scrap_html(html):
    # gavusi html dokumenta funkcija grazina pitoninius duomenis
    # gal cia galima naudoti kazkoki standarta?
    # gaunant reiketu pateikti
    opendatagovlt.scrap_html()
    assert


def test_scrap_xls(xls):
    opendatagovlt.scrap_xls()
    assert


def test_scrap_csv(csv):
    opendatagovlt.scrap_csv(csv)
    assert


def test_scrap_xml(xml):
    opendatagovlt.scrap_xml(xml)
    assert


def test_validate_data():
    opendatagovlt.validate_data()
    #todo What?
    assert


def test_normalize_data():
    opendatagovlt.normalize_data()
    #what is that?
    assert